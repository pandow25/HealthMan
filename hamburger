import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class hamburger here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class hamburger extends parent
{
    int speed = 6;
    
    public void act() 
    {
        hamburger a = new hamburger();
        Actor i = getOneIntersectingObject(hamburger.class);
        move(speed);
        //checkdouble(i);
        //end();
    }    
}
