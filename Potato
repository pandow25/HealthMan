import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class potato here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class potato extends parent
{
    int speed = -2;
    public void act() 
    {
        potato a = new potato();
        Actor i = getOneIntersectingObject(potato.class);
        move(speed);
        //checkdouble(i);
        //end();
    }    
}
